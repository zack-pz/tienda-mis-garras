{
  "name": "tienda-mistrapitos",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "dev": "NX_DAEMON=false nx run-many -t serve -p web,api --parallel=2",
    "serve": "NX_DAEMON=false nx run-many -t serve -p web,api --parallel=2",
    "nx": "nx",
    "graph": "nx graph",
    "affected": "nx affected",
    "docker:dev": "docker compose -f docker/docker-compose.dev.yml up",
    "docker:dev:build": "docker compose -f docker/docker-compose.dev.yml up --build",
    "docker:prod": "docker compose -f docker/docker-compose.prod.yml up --build -d",
    "docker:db": "docker compose -f docker/docker-compose.dev.yml up postgres"
  },
  "devDependencies": {
    "nx": "^21.6.4"
  }
}
